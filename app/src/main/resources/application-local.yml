spring:
  application:
    name: UserManager
  datasource:
    url: jdbc:mysql://localhost:3306/user_manager?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: password
    driver-class-name: com.mysql.jdbc.Driver
server:
  port: 8080